#include <stdio.h>
int main()
{
   long int a[1000005],t,i,j;
   /* for(i=0;i<10;i++)scanf("%d",&ara[i]);
    for(j=0;j<6;j++)scanf("%d",&ara2[j]);
    for(j=0;j<6;j++){
    for(i=0;i<10;i++){
    if(ara2[j]<ara[i]){
    t=ara[i];
    ara[i]=ara2[j];
    ara2[j]=t;
    }
    }
    }
    for(i=0;i<10;i++)printf("%d ",ara[i]);
    printf("\n");
    for(i=0;i<6;i++)printf("%d ",ara2[i]);*/
}

   /*for(i=0;i<n;i++){
    for(j=i+1;j<n;j++){
        if(strlen(str[i])>strlen(str[j])){
              strcpy(ch,str[i]);
           strcpy(str[i],str[j]);
           strcpy(str[j],ch);
        }
        else if(strlen(str[i])==strlen(str[j])){
            d=strcmp(str[i],str[j]);
        if(d>0){
           strcpy(ch,str[i]);
           strcpy(str[i],str[j]);
           strcpy(str[j],ch);
        }
        }
    }
    printf("%s  ",str[i]);
   }
   printf("\n");
   }*/
