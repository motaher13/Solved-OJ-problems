#include <stdio.h>
int main()
{
    int num;
    printf("enter the fucking number:");
    scanf("%d",&num);
    if(num%2==0)printf("fucking number is even");
    if(num%2!=0)printf("asshole number is odd");
    if(num==0)printf("fucker is neither even nor odd but a real bustard");
    return 0;
}
