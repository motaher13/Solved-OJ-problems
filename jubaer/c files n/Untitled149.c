#include <stdio.h>
int main()
{
    int count;
    scanf("%d",&count);
    if(count<100){

    printf("the number is less than 100.");
    printf(" it is the square form :%d",count*count);
    }
    else{
      printf("the number is larger than my pennis");

    }
    return 0;
}
